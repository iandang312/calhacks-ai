# --- Python Agent (agent/ and env/) ---
GEMINI_API_KEY=           # agentspan runtime — get from Google AI Studio
GOOGLE_CLOUD_PROJECT=     # agentspan runtime — your GCP project ID
ANTHROPIC_API_KEY=        # alternative LLM provider — get from console.anthropic.com
MODEL=google_gemini/gemini-2.0-flash  # LLM to use (see agentspan model list)
# DEEPGRAM_API_KEY=       # add when Python agent STT integration is built
